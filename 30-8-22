#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{    
    int i;
    char *s;
    s=malloc(1024*sizeof(char));
    scanf("%[^\n]",s);
    int len=strlen(s);
    for(i=0;i<len;i++)
    {

        if(s[i]!=' ')
        {
            printf("%c",s[i]);
        }
        else
        {
            printf("\n");
        }
        
    }

    return 0;
}

output:-
This is C 
Your Output (stdout)
This
is
C
Expected Output

This
is
C{-truncated-}

